#cloud-config
users:
  - name: ${svc_username}
    passwd: ${svc_password}
    ssh-authorized-keys:
      - ssh-rsa ${public_key}
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
    groups: sudo
    shell: /bin/bash

packages:
  - tree
  - mlocate
  - unzip
